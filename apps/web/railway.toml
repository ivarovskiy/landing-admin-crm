[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm -C apps/web build"

[deploy]
startCommand = "pnpm -C apps/web start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
